const prices = {
    bike: 100,
    egg: 2,
    milk: 10,
    lamb: 20,
    car: 20000
}

const keys = Object.keys(prices);
console.log(keys);

const vals = Object.values(prices);
console.log(vals);

const entries = Object.entries(prices);
console.log(entries);
